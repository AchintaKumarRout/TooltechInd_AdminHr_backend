spring.application.name=TooltechAdmin

# === DATABASE (Postgres) ===
spring.datasource.url=jdbc:postgresql://localhost:5432/Tooltech_final
spring.datasource.username=satyabratadhir
spring.datasource.password=sonu@5697
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# === JWT ===
jwt.secret=726eee81a356976e8a5db158f19c009d

# === SERVER ===
server.port=8081

# === CORS ===
spring.web.cors.allowed-origins=http://localhost:3000,http://localhost:3002
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true

# === LOGGING ===
logging.level.org.springframework.security=DEBUG
logging.level.com.tooltechind=DEBUG

# === FILE UPLOAD ===
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

